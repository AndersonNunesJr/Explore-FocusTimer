const light = document.getElementById("light");
const dark = document.getElementById("dark");
const timerBtnsDarkMode = document.querySelectorAll(".timerBtnsDarkMode");
const btnSoundsDarkMode = document.querySelectorAll(".btnSoundsDarkMode");

const displayTimer = document.getElementById("displayTimer");
const minutesDisplay = document.getElementById("minutes");
const secondsDisplay = document.getElementById("seconds");

const btnSounds = document.querySelectorAll(".btnSounds");
const btnSoundspressed = document.querySelectorAll(".btnSoundspressed");

const btnPressedForest = document.querySelector(".btnPressedForest");
const btnPressedRain = document.querySelector(".btnPressedRain");
const btnPressedCoffeeShop = document.querySelector(".btnPressedCoffeeShop");
const btnPressedFireplace = document.querySelector(".btnPressedFireplace");

const forest = document.querySelector(".forest");
const rain = document.querySelector(".rain");
const coffeeShop = document.querySelector(".coffeeShop");
const fireplace = document.querySelector(".fireplace");

const timerBtns = document.querySelector(".timerBtns");
const btnPlay = document.querySelector(".play");
const btnPause = document.querySelector(".pause");
const btnSet = document.querySelector(".set");
const btnStop = document.querySelector(".stop");
const btnAddTimer  = document.querySelector(".addTimer");
const btnCutTimer = document.querySelector(".cutTimer");


export const elements = {
    light,
    dark,
    timerBtnsDarkMode,
    btnSoundsDarkMode,
    displayTimer,
    minutesDisplay,
    secondsDisplay,
    btnSounds,
    btnSoundspressed,
    btnPressedForest,
    btnPressedRain,
    btnPressedCoffeeShop,
    btnPressedFireplace,
    forest,
    rain,
    coffeeShop,
    fireplace,
    timerBtns,
    btnPlay,
    btnPause,
    btnSet,
    btnStop,
    btnAddTimer,
    btnCutTimer,
}
